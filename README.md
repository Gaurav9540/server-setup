✅ What this install script 

- MySQL (server, client, config, data directory)
- Java JDK 17
- MySQL firewall rule
- The /run/mysqld directory your script created
- All config changes such as mysqld.cnf
- Auto cleanup with autoremove + clean
